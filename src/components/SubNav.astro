---
const base = import.meta.env.BASE_URL;
const { items = [], active = '' } = Astro.props;
---
<ul class="subnav">
  {items.map((it) => (
    <li>
      <a href={base + it.href} aria-current={active === it.key ? 'page' : undefined}>{it.label}</a>
    </li>
  ))}
</ul>
